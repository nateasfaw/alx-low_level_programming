#include <stdio.h>

/*
*	main -> print the size of various types
*	return:0 if it exits properly,non zero otherwise
*/ 
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle")
	return (0);
}
